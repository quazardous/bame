// Settings menu (long-press CENTER). Flat list of user-editable items:
// Capacity, V min utile, V max utile, Eco mode, Reset ALL + read-only Info V.
// Reads global state from bame_state.h.
#pragma once

void settingsMenu();
